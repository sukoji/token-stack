# Contributing

Thank you for improving token-stack. Keep the CLI dependency-free, add a fixture and test for parser changes, and run `npm test` before opening a pull request.

Provider adapters are welcome. Please document the source format, never upload transcripts, and make a provider opt-in until its parsing fixtures cover supported platforms.
